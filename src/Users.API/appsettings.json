{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Defino la cadena de conexión en appsettings.json para que sea más fácil de mantener y cambiar sin necesidad de recompilar la aplicación. Esto también permite separar la configuración del código, lo que es una buena práctica.
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=app.db"
  }

  var connectionString = _config.GetConnectionString("DefaultConnection");
  ?? "Data source = app.db""

}
